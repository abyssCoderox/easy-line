# 安全策略文档

## 文档信息

| 项目名称 | LINE Bot 智能消息处理系统 |
|---------|-------------------------|
| 文档版本 | V1.0 |
| 创建日期 | 2026-03-11 |
| 文档状态 | 待评审 |

---

## 1. 安全概述

### 1.1 安全目标

| 目标 | 描述 |
|------|------|
| 机密性 | 确保敏感数据不被未授权访问 |
| 完整性 | 确保数据在传输和存储过程中不被篡改 |
| 可用性 | 确保服务稳定可用，防止拒绝服务攻击 |
| 可审计性 | 确保所有操作可追溯，便于安全审计 |

### 1.2 安全架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              安全架构层次                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                           网络安全层                                  │   │
│  │  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐          │   │
│  │  │   HTTPS   │ │  防火墙   │ │  DDoS防护 │ │  IP白名单  │          │   │
│  │  └───────────┘ └───────────┘ └───────────┘ └───────────┘          │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                           应用安全层                                  │   │
│  │  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐          │   │
│  │  │ 签名验证  │ │ 身份认证  │ │ 权限控制  │ │ 请求限流  │          │   │
│  │  └───────────┘ └───────────┘ └───────────┘ └───────────┘          │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                           数据安全层                                  │   │
│  │  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐          │   │
│  │  │ 数据加密  │ │ 密钥管理  │ │ 数据脱敏  │ │ 数据备份  │          │   │
│  │  └───────────┘ └───────────┘ └───────────┘ └───────────┘          │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                           运维安全层                                  │   │
│  │  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐          │   │
│  │  │ 日志审计  │ │ 漏洞扫描  │ │ 安全监控  │ │ 应急响应  │          │   │
│  │  └───────────┘ └───────────┘ └───────────┘ └───────────┘          │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 2. 网络安全

### 2.1 HTTPS加密传输

**要求：**
- 所有外部通信必须使用HTTPS
- TLS版本不低于1.2
- 使用强加密套件

**推荐加密套件：**
```
TLS_AES_256_GCM_SHA384
TLS_CHACHA20_POLY1305_SHA256
TLS_AES_128_GCM_SHA256
ECDHE-RSA-AES256-GCM-SHA384
ECDHE-RSA-AES128-GCM-SHA256
```

**Nginx配置示例：**
```nginx
server {
    listen 443 ssl http2;
    
    ssl_certificate /etc/nginx/ssl/cert.pem;
    ssl_certificate_key /etc/nginx/ssl/key.pem;
    
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256;
    ssl_prefer_server_ciphers off;
    
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 1d;
    ssl_session_tickets off;
    
    add_header Strict-Transport-Security "max-age=31536000" always;
}
```

### 2.2 防火墙规则

**入站规则：**

| 端口 | 协议 | 来源 | 说明 |
|------|------|------|------|
| 80 | TCP | Any | HTTP重定向到HTTPS |
| 443 | TCP | Any | HTTPS访问 |
| 22 | TCP | 管理网段 | SSH管理 |

**出站规则：**

| 端口 | 协议 | 目标 | 说明 |
|------|------|------|------|
| 443 | TCP | api.line.me | LINE API |
| 443 | TCP | 第三方API | 外部服务调用 |
| 5432 | TCP | 数据库内网 | PostgreSQL |
| 6379 | TCP | 缓存内网 | Redis |

### 2.3 DDoS防护

**防护措施：**
- 启用Nginx限流模块
- 配置连接数限制
- 启用SYN Cookie
- 使用CDN加速（可选）

**Nginx限流配置：**
```nginx
http {
    limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/s;
    limit_conn_zone $binary_remote_addr zone=conn_limit:10m;
    
    server {
        location /api/ {
            limit_req zone=api_limit burst=20 nodelay;
            limit_conn conn_limit 10;
        }
    }
}
```

### 2.4 IP白名单

**管理接口IP白名单：**
```nginx
location /admin/ {
    allow 10.0.0.0/8;
    allow 192.168.0.0/16;
    deny all;
    
    proxy_pass http://localhost:3000;
}
```

---

## 3. 应用安全

### 3.1 Webhook签名验证

**验证流程：**
```
┌───────────────────────────────────────────────────────────────┐
│                    签名验证流程                                │
├───────────────────────────────────────────────────────────────┤
│                                                               │
│  1. 接收请求                                                  │
│       │                                                       │
│       ▼                                                       │
│  2. 获取 X-Line-Signature 请求头                              │
│       │                                                       │
│       ▼                                                       │
│  3. 使用 Channel Secret 对请求体进行 HMAC-SHA256               │
│       │                                                       │
│       ▼                                                       │
│  4. Base64编码计算结果                                        │
│       │                                                       │
│       ▼                                                       │
│  5. 比较计算签名与请求头签名                                   │
│       │                                                       │
│       ├── 匹配 ──▶ 继续处理请求                               │
│       │                                                       │
│       └── 不匹配 ──▶ 返回 401 Unauthorized                    │
│                                                               │
└───────────────────────────────────────────────────────────────┘
```

**实现代码：**
```typescript
import * as crypto from 'crypto';

export class SignatureGuard implements CanActivate {
  constructor(private configService: ConfigService) {}
  
  canActivate(context: ExecutionContext): boolean {
    const request = context.switchToHttp().getRequest();
    const signature = request.headers['x-line-signature'];
    const body = request.rawBody;
    
    const channelSecret = this.configService.get('LINE_CHANNEL_SECRET');
    const expectedSignature = crypto
      .createHmac('sha256', channelSecret)
      .update(body)
      .digest('base64');
    
    if (signature !== expectedSignature) {
      throw new UnauthorizedException('Invalid signature');
    }
    
    return true;
  }
}
```

### 3.2 身份认证

**管理接口认证：**

采用JWT (JSON Web Token) 认证方式。

**Token生成：**
```typescript
import * as jwt from 'jsonwebtoken';

interface TokenPayload {
  userId: string;
  role: string;
}

export class AuthService {
  generateToken(payload: TokenPayload): string {
    return jwt.sign(payload, process.env.JWT_SECRET, {
      expiresIn: '24h',
      issuer: 'line-bot-system'
    });
  }
  
  verifyToken(token: string): TokenPayload {
    return jwt.verify(token, process.env.JWT_SECRET) as TokenPayload;
  }
}
```

**认证中间件：**
```typescript
@Injectable()
export class JwtAuthGuard implements CanActivate {
  constructor(private jwtService: JwtService) {}
  
  async canActivate(context: ExecutionContext): boolean {
    const request = context.switchToHttp().getRequest();
    const token = this.extractToken(request);
    
    if (!token) {
      throw new UnauthorizedException('Token not found');
    }
    
    try {
      const payload = await this.jwtService.verifyAsync(token);
      request.user = payload;
      return true;
    } catch {
      throw new UnauthorizedException('Invalid token');
    }
  }
  
  private extractToken(request: Request): string | undefined {
    const authorization = request.headers.authorization;
    if (authorization?.startsWith('Bearer ')) {
      return authorization.substring(7);
    }
    return undefined;
  }
}
```

### 3.3 权限控制

**角色定义：**

| 角色 | 权限 |
|------|------|
| admin | 所有管理功能 |
| operator | 查看配置、查看日志 |
| viewer | 仅查看统计数据 |

**权限守卫：**
```typescript
@Injectable()
export class RolesGuard implements CanActivate {
  constructor(private reflector: Reflector) {}
  
  canActivate(context: ExecutionContext): boolean {
    const requiredRoles = this.reflector.get<string[]>(
      'roles',
      context.getHandler()
    );
    
    if (!requiredRoles) {
      return true;
    }
    
    const request = context.switchToHttp().getRequest();
    const user = request.user;
    
    return requiredRoles.some(role => user.role === role);
  }
}
```

### 3.4 请求限流

**限流策略：**

| 接口类型 | 限流规则 | 说明 |
|---------|---------|------|
| Webhook | 无限制 | 来自LINE服务器 |
| 管理接口 | 100次/分钟/IP | 防止暴力破解 |
| 公开接口 | 60次/分钟/IP | 防止滥用 |

**实现代码：**
```typescript
import rateLimit from 'express-rate-limit';

export const apiLimiter = rateLimit({
  windowMs: 60 * 1000,
  max: 100,
  message: {
    code: 3003,
    message: '请求过于频繁，请稍后再试'
  },
  standardHeaders: true,
  legacyHeaders: false
});
```

### 3.5 输入验证

**验证规则：**

| 字段类型 | 验证规则 |
|---------|---------|
| 字符串 | 长度限制、特殊字符过滤 |
| 数字 | 范围限制、类型验证 |
| JSON | 格式验证、深度限制 |
| URL | 格式验证、协议限制 |

**实现示例：**
```typescript
import { IsString, IsInt, IsOptional, Min, Max, MaxLength } from 'class-validator';

export class CreateTaskDto {
  @IsString()
  @MaxLength(100)
  task_name: string;
  
  @IsString()
  @MaxLength(100)
  cron_expression: string;
  
  @IsInt()
  @Min(0)
  @Max(10)
  @IsOptional()
  retry_count?: number;
}
```

---

## 4. 数据安全

### 4.1 敏感数据识别

| 数据类型 | 敏感级别 | 示例 |
|---------|---------|------|
| 密钥 | 高 | Channel Secret, API Key |
| Token | 高 | Access Token, JWT Secret |
| 用户信息 | 中 | LINE用户ID, 昵称 |
| 对话内容 | 中 | 用户消息, 回复内容 |
| 配置信息 | 低 | 系统配置参数 |

### 4.2 数据加密

#### 4.2.1 传输加密

- 所有API通信使用HTTPS
- 数据库连接使用SSL/TLS
- Redis连接支持TLS

#### 4.2.2 存储加密

**API Key加密存储：**
```typescript
import * as crypto from 'crypto';

export class EncryptionService {
  private algorithm = 'aes-256-gcm';
  private key: Buffer;
  
  constructor(secretKey: string) {
    this.key = crypto.scryptSync(secretKey, 'salt', 32);
  }
  
  encrypt(text: string): string {
    const iv = crypto.randomBytes(16);
    const cipher = crypto.createCipheriv(this.algorithm, this.key, iv);
    
    let encrypted = cipher.update(text, 'utf8', 'hex');
    encrypted += cipher.final('hex');
    
    const authTag = cipher.getAuthTag();
    return `${iv.toString('hex')}:${authTag.toString('hex')}:${encrypted}`;
  }
  
  decrypt(encryptedData: string): string {
    const [ivHex, authTagHex, encrypted] = encryptedData.split(':');
    const iv = Buffer.from(ivHex, 'hex');
    const authTag = Buffer.from(authTagHex, 'hex');
    
    const decipher = crypto.createDecipheriv(this.algorithm, this.key, iv);
    decipher.setAuthTag(authTag);
    
    let decrypted = decipher.update(encrypted, 'hex', 'utf8');
    decrypted += decipher.final('utf8');
    
    return decrypted;
  }
}
```

### 4.3 密钥管理

**密钥存储策略：**

| 密钥类型 | 存储方式 | 访问权限 |
|---------|---------|---------|
| 环境变量密钥 | .env文件（不提交Git） | 运维人员 |
| 数据库密钥 | 加密后存储 | 应用程序 |
| JWT密钥 | 环境变量 | 应用程序 |

**密钥轮换策略：**
- Channel Secret：按需更换（建议每90天）
- Access Token：长期有效，按需更换
- JWT Secret：建议每30天更换

### 4.4 数据脱敏

**日志脱敏规则：**
```typescript
export class LogSanitizer {
  private sensitiveFields = [
    'password',
    'token',
    'secret',
    'apiKey',
    'authorization'
  ];
  
  sanitize(data: any): any {
    if (typeof data !== 'object' || data === null) {
      return data;
    }
    
    const sanitized = { ...data };
    
    for (const field of this.sensitiveFields) {
      if (sanitized[field]) {
        sanitized[field] = '***REDACTED***';
      }
    }
    
    return sanitized;
  }
}
```

**用户信息脱敏：**
```typescript
export function maskUserId(userId: string): string {
  if (userId.length <= 8) {
    return '***';
  }
  return userId.substring(0, 4) + '****' + userId.substring(userId.length - 4);
}
```

### 4.5 数据备份

**备份策略：**

| 备份类型 | 频率 | 保留期限 | 加密 |
|---------|------|---------|------|
| 全量备份 | 每日 | 30天 | 是 |
| 增量备份 | 每4小时 | 7天 | 是 |
| 配置备份 | 每次变更 | 永久 | 是 |

**备份加密：**
```bash
pg_dump -U postgres linebot | gzip | openssl enc -aes-256-cbc -salt -out backup_$(date +%Y%m%d).enc
```

---

## 5. 运维安全

### 5.1 日志审计

**审计日志内容：**

| 事件类型 | 记录内容 |
|---------|---------|
| 用户认证 | 登录/登出、Token刷新 |
| 权限变更 | 角色变更、权限修改 |
| 配置变更 | 任务配置、系统配置修改 |
| 敏感操作 | 数据导出、批量操作 |
| 异常事件 | 认证失败、权限拒绝 |

**日志格式：**
```json
{
  "timestamp": "2026-03-11T10:00:00.000Z",
  "level": "info",
  "event": "user_login",
  "user_id": "admin",
  "ip": "192.168.1.100",
  "user_agent": "Mozilla/5.0...",
  "details": {
    "method": "jwt"
  }
}
```

### 5.2 安全监控

**监控指标：**

| 指标 | 告警阈值 | 处理方式 |
|------|---------|---------|
| 认证失败次数 | > 10次/分钟 | IP封禁 |
| 异常请求 | > 100次/分钟 | 流量限制 |
| 敏感操作 | 任何发生 | 实时通知 |
| 系统漏洞 | 发现即告警 | 紧急修复 |

**监控配置示例：**
```yaml
groups:
  - name: security_alerts
    rules:
      - alert: HighAuthFailureRate
        expr: rate(auth_failures_total[5m]) > 0.1
        for: 1m
        labels:
          severity: warning
        annotations:
          summary: "高认证失败率"
          
      - alert: SuspiciousActivity
        expr: rate(suspicious_requests_total[5m]) > 0.05
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "检测到可疑活动"
```

### 5.3 漏洞扫描

**扫描策略：**

| 扫描类型 | 频率 | 工具 |
|---------|------|------|
| 依赖漏洞 | 每次构建 | npm audit / Snyk |
| 代码漏洞 | 每次提交 | SonarQube |
| 基础设施 | 每周 | Nessus |
| 渗透测试 | 每季度 | 人工测试 |

**依赖扫描配置：**
```json
{
  "scripts": {
    "audit": "npm audit --audit-level=moderate",
    "audit:fix": "npm audit fix"
  }
}
```

### 5.4 应急响应

**响应流程：**
```
┌───────────────────────────────────────────────────────────────┐
│                    安全事件响应流程                            │
├───────────────────────────────────────────────────────────────┤
│                                                               │
│  发现安全事件                                                 │
│       │                                                       │
│       ▼                                                       │
│  ┌─────────────┐                                              │
│  │ 初步评估    │  确定事件等级                                 │
│  └──────┬──────┘                                              │
│         │                                                     │
│         ▼                                                     │
│  ┌─────────────┐                                              │
│  │ 遏制措施    │  隔离受影响系统                               │
│  └──────┬──────┘                                              │
│         │                                                     │
│         ▼                                                     │
│  ┌─────────────┐                                              │
│  │ 根因分析    │  确定攻击来源和方式                           │
│  └──────┬──────┘                                              │
│         │                                                     │
│         ▼                                                     │
│  ┌─────────────┐                                              │
│  │ 修复漏洞    │  应用安全补丁                                 │
│  └──────┬──────┘                                              │
│         │                                                     │
│         ▼                                                     │
│  ┌─────────────┐                                              │
│  │ 恢复服务    │  验证系统安全后恢复                           │
│  └──────┬──────┘                                              │
│         │                                                     │
│         ▼                                                     │
│  ┌─────────────┐                                              │
│  │ 事后总结    │  编写事件报告，改进安全措施                   │
│  └─────────────┘                                              │
│                                                               │
└───────────────────────────────────────────────────────────────┘
```

**事件等级定义：**

| 等级 | 定义 | 响应时间 |
|------|------|---------|
| P0-紧急 | 数据泄露、系统被入侵 | 15分钟 |
| P1-高 | 认证绕过、权限提升 | 1小时 |
| P2-中 | 拒绝服务、异常访问 | 4小时 |
| P3-低 | 潜在漏洞、配置问题 | 24小时 |

---

## 6. 安全检查清单

### 6.1 部署前检查

- [ ] HTTPS证书有效
- [ ] 防火墙规则正确配置
- [ ] 敏感信息不在代码中硬编码
- [ ] 数据库连接使用SSL
- [ ] Redis访问需要认证
- [ ] 管理接口需要认证
- [ ] 日志不包含敏感信息
- [ ] 依赖无已知漏洞

### 6.2 运行时检查

- [ ] 签名验证正常工作
- [ ] 限流策略生效
- [ ] 监控告警正常
- [ ] 日志记录完整
- [ ] 备份任务正常执行

### 6.3 定期检查

- [ ] 密钥轮换
- [ ] 依赖更新
- [ ] 漏洞扫描
- [ ] 权限审计
- [ ] 日志审计

---

## 7. 安全配置参考

### 7.1 环境变量配置

```bash
# LINE配置
LINE_CHANNEL_SECRET=your-channel-secret
LINE_CHANNEL_ACCESS_TOKEN=your-access-token

# JWT配置
JWT_SECRET=your-jwt-secret-key
JWT_EXPIRES_IN=24h

# 数据库配置
DATABASE_URL=postgresql://user:password@localhost:5432/linebot?ssl=true

# Redis配置
REDIS_URL=redis://:password@localhost:6379

# 加密配置
ENCRYPTION_KEY=your-encryption-key

# 大模型配置
LLM_API_KEY=your-llm-api-key
```

### 7.2 安全响应头配置

```typescript
app.use(helmet({
  contentSecurityPolicy: {
    directives: {
      defaultSrc: ["'self'"],
      scriptSrc: ["'self'"],
      styleSrc: ["'self'", "'unsafe-inline'"],
      imgSrc: ["'self'", 'data:', 'https:'],
      connectSrc: ["'self'", 'https://api.line.me'],
      fontSrc: ["'self'"],
      objectSrc: ["'none'"],
      frameSrc: ["'none'"]
    }
  },
  crossOriginEmbedderPolicy: true,
  crossOriginOpenerPolicy: true,
  crossOriginResourcePolicy: { policy: 'same-origin' },
  hsts: {
    maxAge: 31536000,
    includeSubDomains: true,
    preload: true
  }
}));
```

---

## 8. 附录

### 8.1 安全相关法规参考

- 《个人信息保护法》
- 《网络安全法》
- LINE Messaging API使用条款
- GDPR（如涉及欧盟用户）

### 8.2 修订历史

| 版本 | 日期 | 修订人 | 修订内容 |
|------|------|--------|---------|
| V1.0 | 2026-03-11 | - | 初始版本 |

---

**文档评审意见：**

| 评审人 | 评审日期 | 评审意见 | 状态 |
|--------|---------|---------|------|
|        |         |         | 待评审 |
